<%= form_for(trumpet) do |f| %>
  <% if trumpet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trumpet.errors.count, "error") %> prohibited this trumpet from being saved:</h2>

      <ul>
      <% trumpet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_field :note %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
